"""
阿里云 DashScope（千问）专用配置助手

集中定义 DashScope 相关的模型常量、API 端点、以及
视觉分析与图像生成的默认参数，供 LLM Provider 层调用。
"""

# ============================================================
# 模型常量
# ============================================================

# 视觉理解模型：用于分析图片内容（主体、场景、情绪等）
MODEL_VISION = "qwen-vl-plus"

# 图像生成模型：用于图生图 / 文生图
MODEL_IMAGE = "wanx-v1"

# 文本生成模型：用于提示词优化
MODEL_TEXT = "qwen-turbo"


# ============================================================
# API 端点
# ============================================================

# DashScope 服务基础地址
BASE_URL = "https://dashscope.aliyuncs.com"

# 文生图接口
TEXT_TO_IMAGE_URL = f"{BASE_URL}/api/v1/services/aigc/text2image/image-synthesis"

# 图生图接口
IMAGE_TO_IMAGE_URL = f"{BASE_URL}/api/v1/services/aigc/image2image/image-synthesis"

# 异步任务查询接口（使用时拼接 task_id）
TASK_QUERY_URL_TEMPLATE = f"{BASE_URL}/api/v1/tasks/{{task_id}}"


# ============================================================
# 图像尺寸（宽*高，像素）
# ============================================================

# 各输出比例对应的默认生成尺寸
IMAGE_SIZES = {
    "3:4": "768*1024",    # 竖构图（默认，SKILL.md 推荐）
    "3:5": "768*1280",    # 竖版海报（实景拼贴）
    "2:3": "768*1152",    # 竖版海报（冰箱贴/城市海报）
    "1:1": "1024*1024",   # 正方形
    "4:3": "1024*768",    # 横构图
    "16:9": "1280*720",   # 宽屏
}

# 默认输出尺寸（3:4 竖图）
DEFAULT_IMAGE_SIZE = IMAGE_SIZES["3:4"]


# ============================================================
# 图像生成默认参数
# ============================================================

DEFAULT_GENERATE_PARAMS = {
    "size": DEFAULT_IMAGE_SIZE,  # 输出尺寸
    "n": 1,                      # 生成数量
    # "seed": 可选，传入随机种子用于复现结果
}


# ============================================================
# 视觉分析默认参数
# ============================================================

# 视觉分析的系统提示词，引导模型输出结构化 JSON
VISION_ANALYSIS_SYSTEM_PROMPT = (
    "你是一个专业的图片内容分析师。请分析给定图片，并按以下维度输出结构化 JSON：\n"
    "- subject: 主体（如 cat、person、building）\n"
    "- scene: 场景（如 indoor、outdoor、night）\n"
    "- mood: 情绪关键词（如 quiet、warm、lively）\n"
    "- composition: 构图（如 close-up、wide-shot）\n"
    "- colors: 主色调列表\n"
    "- key_objects: 关键物件列表\n"
    "请仅输出 JSON，不要包含其他说明文字。"
)

# 视觉分析请求默认参数（调用时需额外传入 image 字段）
VISION_ANALYSIS_PARAMS = {
    "model": MODEL_VISION,
}


# ============================================================
# 照片复兴分析系统提示词
# ============================================================

# 用于 Photo Revival 技能的深度分析提示词
# 要求 VL 模型分析图片并生成结构化 JSON，包含：
# 主体识别、核心元素、插画规则、最终英文提示词、诗意小字选项
REVIVAL_ANALYSIS_SYSTEM_PROMPT = """你是一位专业的照片复兴插画分析师。请仔细分析给定照片，并按以下结构输出 JSON：

{
  "category": "portrait 或 landscape（根据画面主体判断：以人物/人像/宠物/室内近景为主填 portrait，以城市/建筑/山川/风景/自然风光为主填 landscape）",
  "subject_analysis": "详细描述照片主体（人物外貌、穿着、表情、动作、场景环境、氛围感受等，200-400字中文）",
  "core_elements": ["需要保留的核心元素1", "元素2", ...],
  "rules": {
    "composition": "构图说明（如 3:4 竖版）",
    "main_area": "主插画区域占比（如 约占画面 14%）",
    "negative_space": "留白比例（如 约 86% 的纯白冷压纸面）",
    "style": "笔触风格（如 手绘水彩 + 铅笔线，纸张纹理清晰可见）",
    "colors": ["画面中出现的颜色描述1", "颜色2", ...],
    "text_note": "画面文字说明（如 画面下方一处手写小字）",
    "avoid": "需要避免的内容"
  },
  "special_notes": "关于原图特殊元素的处理建议（如菜单文字不照抄，只保留色块信号等）",
  "final_prompt": "最终用于 AI 图像生成的英文提示词（300-600字，直接可用，包含完整的画面描述、风格、色彩、构图等）",
  "poetic_options": ["诗意小字备选1", "备选2", "备选3", "备选4", "备选5"],
  "suggestions": ["使用建议1", "建议2", ...]
}

要求：
1. subject_analysis 要详细、生动，捕捉照片中的关键视觉特征
2. core_elements 提取 3-5 个必须保留的特征点
3. final_prompt 必须是英文，直接可用于 Midjourney/DALL·E/Qwen-Image 等模型
4. final_prompt 中要包含：画面风格、构图比例、主体描述、色彩、留白、纸张纹理、手绘风格等关键要素
5. poetic_options 提供 5 个简短诗意小字（4-8字），风格与照片内容相关
6. 如果画面中有文字招牌等，final_prompt 中只保留色块信号，不抄具体文字
7. 请仅输出 JSON，不要包含其他说明文字或 markdown 代码块标记。"""


# ============================================================
# 城市风景海报分析系统提示词
# ============================================================

# 用于 City Editorial Poster 技能的深度分析提示词
# 针对城市建筑/山川湖泊/自然风光照片，输出面向编辑海报的结构化结果
EDITORIAL_ANALYSIS_SYSTEM_PROMPT = """你是一位专业的城市/风景编辑海报分析师。请仔细分析给定的城市或风景照片，并按以下结构输出 JSON：

{
  "category": "landscape（本技能专用于城市/风景照片，固定填 landscape）",
  "subject_analysis": "详细描述照片主体（建筑、山川、天空、云层、月亮、街道、灯光、湖水等，200-400字中文），突出画面最具辨识度的视觉符号与氛围",
  "core_elements": ["需要保留的核心辨识度元素1（如：某桥轮廓/某天际线形状/云层方向/月亮位置）", "元素2", "元素3", "元素4", "元素5"],
  "rules": {
    "composition": "构图说明（如 2:3 竖版上下分区海报，真实摄影在上，极简插画在下）",
    "top_half": "上半部分真实摄影描述（如：保留原构图、天空层次、建筑轮廓与灯光，仅做电影感低饱和调色）",
    "bottom_half": "下半部分插画提炼说明（如：建筑简化几何色块/云层横向柔笔/远景矩形剪影/新月或灯光以暖黄小点点缀）",
    "style": "插画笔触风格（如 soft gouache + dry brush texture，水彩半透明，纸张颗粒，哑光印刷）",
    "colors": ["画面色彩提炼：warm ivory / muted navy / blue grey / smoky grey / warm ochre / mustard yellow / soft beige 等"],
    "typography": "底部杂志式英文排版建议（主标题内容、副标题内容、左对齐克制字号、衬线字体、深蓝灰色主标题+斜体serif副标题）",
    "avoid": "需要避免的内容：明显描边/卡通/赛博朋克/3D/矢量图标感、商业广告俗套装饰、过度HDR与强锐化"
  },
  "special_notes": "特殊元素处理建议（如：原照片中的文字招牌不要照抄，只保留色彩块信号）",
  "final_prompt": "最终用于 AI 图像生成的完整英文提示词（400-700字）。必须包含：2:3竖版海报、上下分区、上半真实摄影电影感调色（保留原构图/天空/建筑/灯光/云层）、下半极简几何水粉插画提炼（建筑→色块、远景→剪影、云层→横笔、灯光→暖黄小点）、整体配色warm ivory+muted navy+blue grey+smoky grey+warm ochre+mustard yellow+soft beige低饱和度、米白色大量留白、底部杂志式衬线英文排版主标题+副标题（根据照片内容自动生成英文标题与副标题）、安静诗意现代疏朗、纸张印刷颗粒感、独立艺术杂志/摄影集/艺术书封面/博物馆海报质感。最后必须追加：Vertical poster, 2:3 ratio, high resolution, premium editorial composition。",
  "poetic_options": ["5个中文诗意短句（4-10字），呼应照片内容的安静/克制/现代情绪，如：/城市在呼吸/云停在屋顶/夜色是蓝色/桥上有风过/"],
  "suggestions": ["使用建议1（如：若模型偏写实，追加 editorial poster layout、split composition、soft gouache illustration 等关键词）", "建议2（如：若插画过于具象，追加 minimal geometric reduction、generous negative space、muted color palette 等关键词）"]
}

要求：
1. final_prompt 必须完整用英文写出，可直接复制给图像模型使用（2:3竖版、上下分区、真实摄影+水粉插画提炼、底部英文衬线排版、色彩、留白、质感、杂志美学缺一不可）
2. final_prompt 中要自动根据照片内容生成"英文主标题"与"英文副标题"写入排版部分
3. poetic_options 提供 5 个中文短句（4-10字），安静克制现代
4. 如果画面中有文字招牌或标识，final_prompt 中只保留色块信号，不抄具体文字
5. 请仅输出 JSON，不要包含其他说明文字或 markdown 代码块标记。"""


# ============================================================
# 照片抽象编辑分析系统提示词
# ============================================================

# 用于 Photo Abstract Editorial 技能的深度分析提示词
# 针对"原照片区域 + 抽象记忆面板 + 诗意标题"的编辑作品风格
ABSTRACT_ANALYSIS_SYSTEM_PROMPT = """你是一位专业的照片抽象编辑分析师。请仔细分析给定照片，提炼其空间事实与视觉关系，并按以下结构输出 JSON：

{
  "category": "portrait 或 landscape（根据画面主体判断：以人物/人像/宠物/室内近景为主填 portrait，以城市/建筑/山川/风景/自然风光为主填 landscape）",
  "subject_analysis": "详细描述照片主体（主体对象、空间关系、方向、曲线、重复节奏、遮挡层级、明暗层级、色彩角色、留白分布等，200-400字中文）",
  "core_elements": ["需要保留的核心空间事实1（如：建筑垂直收分与天空留白的关系）", "元素2", "元素3", "元素4", "元素5"],
  "rules": {
    "composition": "拼接构图说明（如 竖版编辑作品，上为照片区域下为象牙色抽象面板，比例按照片方向自适应）",
    "photo_region": "照片区域处理（如：忠实保留原图，仅等比缩放或轻微裁切，不重画不修饰）",
    "abstract_panel": "抽象面板提炼说明（如：象牙色 #F3F0E8 面板，极简标记系统，母题占面板宽 30%-42%，保留 65%-80% 留白）",
    "style": "抽象笔触风格（如 平面或微有机色块 / 柔和圆形 / 弧形笔触 / 层叠色带，最多两个辅助标记家族）",
    "colors": ["色彩提炼：从原照片提取，降低饱和度，一个主色+一个深色结构色+一个浅色中性色+最多一两个小面积强调色"],
    "typography": "英文衬线标题建议（2-5词主标题，可选3-7词斜体副标题，颜色取照片较深克制的主体色，位于面板母题下方或侧边）",
    "avoid": "需要避免的内容：照片重画/场景重建/滤镜感/海报化照片/矢量描摹/完整插画/信息图/通用图标/密集装饰/渐变阴影纹理噪点"
  },
  "special_notes": "特殊元素处理建议（如：标志性建筑保留一至三个最低限度身份特征，人群用连续短竖墨点，不绘制面部四肢细节）",
  "final_prompt": "最终用于 AI 图像生成的完整英文提示词（400-700字）。必须包含：上方照片区域忠实保留原图、下方象牙色 #F3F0E8 抽象面板、DECONSTRUCT→SELECTIVE PRESERVATION→ABSTRACT/DISTILL→RECONSTRUCT 方法、标记系统、色彩从原照片提取且低饱和、英文衬线主标题（根据照片内容自动生成2-5词）+可选斜体副标题、面板背景无渐变阴影纹理、整体安静诗意现代留白充足。最后必须追加：Vertical editorial composition, 3:4 ratio, high resolution, premium editorial aesthetic。",
  "poetic_options": ["5个中文诗意短句（4-10字），呼应照片空间关系与情绪的安静克制诗意，如：/屋顶在呼吸/桥影入静水/月光分两行/风停在此刻/"],
  "suggestions": ["使用建议1（如：若模型未保留照片区域，追加 preserve the original photograph region faithfully, no repaint）", "建议2（如：若抽象面板出现纹理或渐变，追加 CLEAN uniform ivory background #F3F0E8, no texture, no gradient, no noise）"]
}

要求：
1. final_prompt 必须完整用英文写出，可直接复制给图像模型使用（照片区域+抽象面板+英文衬线标题、方法、标记、色彩、留白、杂志美学缺一不可）
2. final_prompt 中要自动根据照片内容生成"英文主标题"与"可选英文副标题"
3. poetic_options 提供 5 个中文短句（4-10字），安静克制诗意
4. 抽象面板必须是均匀中性象牙色，无渐变、阴影、纹理、噪点
5. 请仅输出 JSON，不要包含其他说明文字或 markdown 代码块标记。"""


# ============================================================
# 异步任务轮询配置
# ============================================================

# 任务轮询间隔（秒）
TASK_POLL_INTERVAL = 2

# 任务最大等待时长（秒），超时则降级或报错
TASK_MAX_WAIT = 120


# ============================================================
# 工具函数
# ============================================================

def get_image_size(ratio: str = "3:4") -> str:
    """
    根据输出比例获取 DashScope 图像生成尺寸字符串。

    Args:
        ratio: 输出比例，如 "3:4"、"1:1"、"4:3"、"16:9"

    Returns:
        尺寸字符串（如 "768*1024"）；未知比例回退到默认 3:4 竖图。
    """
    return IMAGE_SIZES.get(ratio, DEFAULT_IMAGE_SIZE)
