# ====================================================================
# PhotoStyle 后端 Python 依赖
# 安装： pip install -r requirements.txt
# ====================================================================

# Web 框架与 ASGI 服务器
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 数据库 ORM 与迁移（异步）
sqlalchemy[asyncio]>=2.0.0
aiomysql>=0.2.0
alembic>=1.13.0

# 数据校验与配置管理
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
email-validator>=2.2.0

# 表单与文件上传
python-multipart>=0.0.9

# HTTP 客户端（调用 LLM API）
httpx>=0.27.0

# 图片处理
pillow>=10.4.0

# 阿里云 DashScope（千问）SDK
dashscope>=1.20.0

# 对象存储 SDK（MinIO + 阿里云 OSS）
minio>=7.2.0
oss2>=2.18.0

# Redis 客户端（缓存/任务队列）
redis>=5.2.0

# 认证与加密
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
